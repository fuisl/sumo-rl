# @package _global_
defaults:
  - /base
  - /scenario: resco_cologne3
  - /algorithm: libsignal_phase5
  - _self_

experiment:
  name: resco_cologne3__libsignal_mplight
  seed: 42
  seeds: [1]
  runs: 1
  episodes: 1
  total_timesteps: 0
  eval_episodes: 0

algorithm:
  params:
    libsignal_agent: mplight
    libsignal_network: cologne3

env:
  factory: sumo_env
  kwargs:
    single_agent: false
    num_seconds: 3600
    use_gui: false
