# @package _global_
defaults:
  - /base
  - /scenario: resco_ingolstadt21
  - /algorithm: fixed_time
  - _self_

experiment:
  name: ${scenario.name}__fixed_time
  seeds: [1, 2, 3, 4, 5]
  runs: 5
  episodes: 1
  eval_episodes: 5
  eval_seeds: ${experiment.seeds}
  fixed_ts: true

env:
  kwargs:
    num_seconds: 3600
    fixed_ts: true
