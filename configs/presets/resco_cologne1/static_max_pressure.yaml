# @package _global_
defaults:
  - /base
  - /scenario: resco_cologne1
  - /algorithm: static_max_pressure
  - _self_

experiment:
  name: resco_cologne1__static_max_pressure
  seeds: [1, 2, 3, 4, 5]
  runs: 5
  episodes: 1
  fixed_ts: false
  eval_episodes: 0

env:
  factory: sumo_env
  kwargs:
    num_seconds: 3600
