# @package _global_
defaults:
  - /base
  - /scenario: resco_cologne1
  - /algorithm: q_learning
  - _self_

experiment:
  name: ${scenario.name}__ql
  seed: 42
  runs: 1
  episodes: 5
  fixed_ts: false
  total_timesteps: 0
  eval_episodes: 0

env:
  factory: sumo_env
  kwargs:
    single_agent: false
    num_seconds: 3600
    use_gui: false
    add_per_agent_info: true
